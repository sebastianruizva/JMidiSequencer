JMidiUtils:
JMidiTrack track;

...

  // Returns the last tick of the last note in this track.
  int getMaxTick(...)

  // Gets the String value of a MidiEvent.
  String getNoteValue(...)

  // Returns a map of a MidiEvents in this track, sorted by tick/beat.
  HashMap<Integer, HashMap<Integer, JMidiEvent> getMidiEvent(...)


